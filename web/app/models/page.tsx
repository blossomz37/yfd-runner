export default function ModelsPage() {
  return (
    <section className="empty-note">
      Structured model editing and step assignment belong here. The backend contract for `GET/PUT /api/step-settings`
      is already in place.
    </section>
  );
}
