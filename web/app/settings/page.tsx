export default function SettingsPage() {
  return (
    <section className="empty-note">
      Config editing, output-directory defaults, and structured step settings can be grouped into this settings surface.
    </section>
  );
}
