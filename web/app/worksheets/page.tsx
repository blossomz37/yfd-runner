export default function WorksheetsPage() {
  return (
    <section className="empty-note">
      Worksheet section browsing and editing are intentionally deferred until after the first vertical slice.
    </section>
  );
}
